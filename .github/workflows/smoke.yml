name: smoke

on:
  push:
    branches: [main]
  pull_request:

jobs:
  smoke:
    name: scripts/smoke.sh
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/smoke.sh
