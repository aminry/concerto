# Throwaway spike — commit source + lockfile, never native build output.
node_modules/
.expo/
dist/
web-build/

# Native build dirs (regenerated by `expo prebuild` / `expo run:*`)
ios/
android/

# Metro / Expo caches
.metro-health-check*
*.log
expo-env.d.ts

# macOS
.DS_Store
