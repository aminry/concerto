// Placeholder proto file. Exists only so `tonic_build::configure().compile_protos(...)`
// has at least one input and `tonic::include_proto!("concerto.v1")` finds a
// generated `concerto.v1.rs`. Removed in Task 07 when the first real messages
// land.
//
// prost-build / tonic-build skip emitting a Rust file for a package that
// contains no messages and no services, so we include one trivial empty
// message here as the carrier. It is not referenced anywhere and goes away
// with Task 07.

syntax = "proto3";

package concerto.v1;

message _Placeholder {}
