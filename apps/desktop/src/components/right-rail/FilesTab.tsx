// Right-rail Files tab — V0.1 stub. The allow/deny + scratch-file
// surface from `design/15 §3.4` lands in a later phase.

export function FilesTab(): JSX.Element {
  return (
    <div className="p-3">
      <p className="text-xs text-slate-400">
        Files panel arrives with the filesystem allow/deny surface.
      </p>
    </div>
  );
}
