// Right-rail Todos tab — V0.1 stub. The persistent todo surface lands
// alongside the Maestro work in a later phase; this card keeps the
// vertical tab strip honest in the meantime.

export function TodosTab(): JSX.Element {
  return (
    <div className="p-3">
      <p className="text-xs text-slate-400">
        Todos arrive with Maestro (V1.0). The panel will list the active
        workarea's pending items.
      </p>
    </div>
  );
}
