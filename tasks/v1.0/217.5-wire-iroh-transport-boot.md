# Task 217.5 — Wire the Iroh Transport Into Core Boot (serve_iroh spawn + pairing responder + live SessionCloser)

| Field | Value |
|---|---|
| Phase | 2 |
| Task type | rust |
| Verification tier | 2 |
| Size | medium (1–3d) |
| Depends on | 212, 217, 207, 208, 209, 210, 211 |
| Touches subsystem(s) | 11 (Remote Transport — boot wiring), 12 (Security — pairing responder), 10 (Client API over Iroh) |
| Smoke gate | unchanged |

## Goal
Make the Iroh transport **actually live on a booted Core**. Tasks 212 (the `crates/transport` endpoint + `serve_iroh`) and 217 (the `TransportHandle` façade) built and froze the spine but both **explicitly deferred wiring it into the boot actor** — so today `concerto_core::api_server::serve_iroh` is called from exactly one integration test (`crates/core/tests/transport_iroh.rs`) and **never from `boot.rs`**, and `boot.rs` installs a `NoopSessionCloser` with a comment saying "Task 217's TransportHandle replaces this one-liner." The result: a Core booted by the daemon/embedded path listens **only on UDS**; there is no live Iroh listener to dial, no Core-side responder for the dedicated Noise-XX **pairing channel** (transport channel-tag `0x03`), and revocation cannot tear down remote sessions. This blocks Task 220's split-host capstone smoke **and** the Phase-2 Tier-3 manual checklist ("pair a real second machine over LAN", "pair from a real remote network", "revoke a device and confirm <60s stream teardown"), all of which require the transport to be live. This task closes that gap: boot brings up `serve_iroh` (config-gated), a Core-side pairing responder accepts the `0x03` channel and completes pairing via `PairingCoordinator`, and the real `TransportHandle`-backed `SessionCloser` replaces the no-op so revoke severs Iroh sessions.

## Inputs to read before starting
- `tasks/v1.0/212-transport-iroh-endpoint.md` + its merged **Handoff Notes** — the `crates/transport` `api.rs` surface (`IrohTransport`, `serve_iroh`'s `ApiDispatcher`, `listen_pairing`, `PairingListener`, channel tags `0x01 Api`/`0x02 PushHint`/`0x03 Pairing`, the X25519 Noise static the transport owns via `start(core_noise_static_private)`), and that boot auto-spawn was deferred here.
- `tasks/v1.0/217-transport-handle-api.md` + its Handoff — the `TransportHandle` 9-method façade (`start`/`stop`/`listen_pairing`/`current_relay`/`switch_relay`/`nat_stats`/`close_sessions_for_device`/`send_wakeup_hint`) and the `DeviceId` ↔ 209 `SessionCloser` reconciliation (`From<[u8;32]>`).
- `tasks/v1.0/207-pairing-noise-xx.md` + `crates/core/src/security/pairing.rs` — the `PairingCoordinator` (`start_pairing` → `PairingChallenge`, the Noise XX over the 32-byte token, the signed `pairing_token || nonce || device_pubkey` verification, `complete_pairing` → `SignedDeviceCert`). Note how `crates/core/tests/pairing.rs` drives the coordinator over a `tokio::io::duplex` — you replace that synthetic duplex with the **real** `0x03` Iroh pairing channel.
- `tasks/v1.0/209-devices-service.md` + `crates/core/src/security/devices.rs` — the `SessionCloser` trait (`close_sessions_for_device(&self, device_id: [u8;32])`) and `NoopSessionCloser`. `tasks/v1.0/210-auth-middleware.md` — the auth path that already validates the device cert on Iroh requests (authenticated RPCs work once paired; pairing itself rides the `0x03` channel, **not** the authenticated gRPC path, so no auth-interceptor change is needed — confirm this).
- `crates/core/src/boot.rs` — the boot actor; the `identity_subsystems` block (issuer + `PairingCoordinator` + `DeviceManager` + revoked-set), the `NoopSessionCloser` line, and the keychain-loaded Core identity. `crates/core/src/api_server.rs` — `serve_iroh`, `CoreServiceSet`/`add_core_services`, `IrohDispatcher`, the `ConnTransport(Iroh)` tag.
- `crates/core/tests/transport_iroh.rs` — the existing real-`serve_iroh` loopback test (relays disabled, two endpoints, asserts `transport_kind == IROH` with a device cert). Your new wiring is verified by an extension of this style.

## Scope — in
- **Boot-time Iroh listener.** In `boot.rs`, when (a) a keychain-backed Core identity exists, (b) `managed.json` `remote_disabled()` is false, and (c) Iroh serving is enabled (a config/env opt-in — default **off** in V1.0 so the existing UDS-only boot smoke is unchanged; e.g. `CONCERTO_ENABLE_IROH=1` or a `managed.json`/runtime-config field — pick one, document it, freeze the name), construct the Core's Noise X25519 static (derive + persist its 32 bytes; reuse the keychain via a new `SecretKind`-or-`CoreSecretSlot`-style slot, or `concerto-identity`'s key tooling — keep it distinct from the Ed25519 identity, per Task 208/212), build the `IrohTransport`/`TransportHandle`, and spawn `serve_iroh` feeding the **shared** `CoreServiceSet` (identical handlers to UDS, with the `ConnTransport(Iroh)` tag). Run it as a supervised sibling of the UDS server under the same shutdown token; clean teardown on shutdown (no leaked endpoint).
- **Core-side pairing responder.** Accept the transport's `0x03` Pairing channel (`PairingListener::accept()` from 212/217), run the **Noise XX responder** over the one-shot token, drive the `PairingCoordinator` to verify the device's signed `PairingRequest` and mint a `SignedDeviceCert`, and return it to the device — the responder half of 207's exchange, now over the real Iroh channel instead of the test `duplex`. Honor the token TTL/one-shot/≤3-active rules already enforced by the coordinator. Audit via the existing `DevicePairing*` kinds.
- **Live `SessionCloser`.** Replace `NoopSessionCloser` in `boot.rs` with an adapter backed by `TransportHandle::close_sessions_for_device` (via the 217 `DeviceId` `From<[u8;32]>`), so `DeviceManager::revoke_device` actually severs the revoked device's open Iroh sessions (the 209/210 revoke→teardown contract).
- **Expose the listener's address for clients/tests.** Make the booted Iroh endpoint id (and the Core's Noise public key) queryable/loggable so a client (Task 220's smoke driver, the Desktop) can dial it — e.g. surface via the existing `Runtime`/`Devices` `GetCoreInfo`/logs, or a boot-returned handle field. Do not invent a new public RPC if an existing one suffices.
- **Tests (Tier 2, loopback, relays disabled):** extend `crates/core/tests/transport_iroh.rs` (or a new `crates/core/tests/iroh_boot.rs`) to: boot a Core with Iroh enabled (keychain-isolated via `CONCERTO_KEYCHAIN_SERVICE`), have a second in-process Iroh endpoint **pair over the `0x03` channel** and receive a cert, then make an **authenticated** `GetServerCapabilities` asserting `transport_kind == IROH`, and a `RevokeDevice` that tears the session down. All waits timeout-bounded; no leaked processes.

## Scope — out
- The split-host **smoke capability** itself (that is Task 220, which consumes this).
- mDNS publish boot-wiring (213 deferred its `publish_mdns` here-or-217 — if trivial to fold in, note it; otherwise leave to 213's follow-up and flag).
- The relay/WSS deploy path, QUIC migration telemetry surfacing beyond what's shipped, Desktop live-dial UI (601), mobile/web.
- Changing any **frozen** proto / `crates/transport` `api.rs` / pairing wire contract — you wire the existing shipped surfaces together; you do not reshape them. If a frozen surface genuinely can't be wired without a change, STOP and ask.

## Public interface this task locks
- **The Iroh-enable config/env toggle** name + default (default **off** in V1.0) — operators script against it; freeze the spelling.
- **The Core-side pairing-responder behavior**: a device connecting on the `0x03` channel with a valid one-shot token completes Noise XX and receives its `SignedDeviceCert` — the observable end-to-end pairing-over-Iroh contract Task 220 + mobile/web pairing rely on. The wire framing is already frozen by 207/212; this task locks that the **Core answers** it.
- The **boot ordering**: identity established → revoked-set mirrored (210) → Iroh listener + pairing responder spawned → ready. Pairing/auth must not be reachable before the revoked-set mirror is in place.

## Implementation notes
- Keep the Iroh spawn **opt-in and non-default** so every existing smoke check + the daemon's default behavior is byte-unchanged; 220's smoke flips the toggle. This avoids regressing the 16-capability smoke suite.
- The Core Noise X25519 static: it must persist (so a Core keeps its identity across reboots) and is distinct from the Ed25519 signing identity (208/212). Reuse the keychain (a new slot) or the Core's data dir; freeze the storage location. Best-effort/timeout-bounded keychain access (mirror Task 206's pattern) so a keychain-less/headless env degrades gracefully rather than hanging.
- The pairing responder is a supervised task that loops on `PairingListener::accept()`; one accept == one pairing attempt == one `0x03` bidi stream (212's channel model). Bound it with the coordinator's TTL.
- Reuse `serve_iroh` + `CoreServiceSet` exactly as `transport_iroh.rs` does — do not duplicate the service-registration graph; factor if needed but prefer calling the shipped builder.
- **Keychain-in-CI hazard:** any test that boots a Core must set `CONCERTO_KEYCHAIN_SERVICE` to a unique value first (see `crates/core/tests/embedded_boot.rs`); the shared `concerto-test-harness` already self-isolates.
- Cross-platform: must build on the Windows + Linux lanes (iroh is cross-platform; the lockfile's `wmi`→`windows 0.62.2` alignment must not regress).

## Verification
Tier 2. **Double:** two Iroh endpoints on one host, relays disabled (direct loopback), a real booted Core with Iroh enabled + keychain-isolated. Proves boot spawn + pairing-over-`0x03` + authenticated IROH RPC + revoke teardown — in CI on one machine. Does **NOT** cover real cross-machine split-host, real NAT/relay, migration, or throughput budgets (Phase-2 Tier-3 checklist, design/11 §10).
1. `cargo check --workspace` clean.
2. `cargo clippy --workspace --all-targets -- -D warnings` clean.
3. `cargo test --workspace --no-fail-fast` → all pass, including the new boot-Iroh pairing+auth+revoke test.
4. `cargo deny check` green; `cargo fmt --all --check` clean.
5. `./scripts/regen-interfaces.sh && git diff --exit-code docs/interfaces/` → clean or committed (if a small surface/`GetCoreInfo` field is added).
6. `scripts/smoke.sh` → **unchanged and still green** (the default-off Iroh toggle keeps every existing capability byte-identical; exit 0).

## Definition of Done
- [x] Boot spawns `serve_iroh` + the pairing responder when enabled (default off); UDS-only boot unchanged when off
- [x] A device pairs over the real `0x03` Iroh channel (Noise XX → `PairingCoordinator` → `SignedDeviceCert`) and then makes an authenticated `transport_kind == IROH` RPC — proven by a loopback test
- [x] `NoopSessionCloser` replaced by the live `IrohTransport`-backed `SessionCloser`; revoke severs Iroh sessions (test)
- [x] Core Noise X25519 static persisted (distinct from Ed25519 identity); storage location frozen + documented
- [x] Iroh-enable toggle name + default frozen; existing smoke suite unchanged + green
- [x] Verification commands pass; interfaces clean/committed
- [x] No `TODO`/`FIXME`/`unimplemented!()`/`todo!()` in new code
- [x] Single commit with the message below

## Outputs
- `crates/core/src/boot.rs` (modified — Iroh listener + pairing responder spawn; live `SessionCloser`)
- `crates/core/src/api_server.rs` (modified only if the spawn helper / `GetCoreInfo` field needs it) + `crates/core/src/security/` (new — the pairing-responder glue module, e.g. `iroh_pairing.rs`)
- `crates/keychain/src/*` *(modified only if a new Core-Noise-static slot is added — additive, frozen)* or the Core data-dir storage for the X25519 static
- `crates/core/tests/iroh_boot.rs` (new — boot+pair+auth+revoke over loopback Iroh) *(or extend `transport_iroh.rs`)*
- `docs/interfaces/*` (regenerated if a surface changed)

## Commit message
```
phase-2: wire Iroh transport into Core boot (serve_iroh + pairing responder)

Boot now spawns serve_iroh (config-gated, default off) feeding the shared
handler set, a Core-side Noise-XX responder accepts the 0x03 pairing
channel and mints device certs via PairingCoordinator, and the real
TransportHandle-backed SessionCloser replaces NoopSessionCloser so revoke
severs Iroh sessions. Makes the transport spine live end-to-end (the
deferral 212/217 left), unblocking the 220 capstone + the Phase-2 Tier-3
checklist.

Refs: tasks/v1.0/217.5-wire-iroh-transport-boot.md
```

## Handoff Notes (filled in when finishing)

**Drift from plan.**
- Wired `serve_iroh` over a **raw `Arc<IrohTransport>`** (built directly via `IrohTransport::start`) rather than via the 217 `TransportHandle` façade. Reason: `TransportHandle::start` spawns *its own* serve loop with the dispatcher, which would conflict with `serve_iroh` (the task's explicitly-named reuse target). The live `SessionCloser` is therefore backed by `IrohTransport::close_sessions_for_device` directly (the same frozen `[u8;32] → DeviceId` reconciliation 217 specified), not the façade method — functionally identical, no frozen surface touched. The `TransportHandle` façade is unused by boot; it remains the seam for 218's Desktop client.
- Two extra files inside the task's `crates/core/src/security/` Output umbrella: `security/identity.rs` (added the `load_or_create_core_noise_static` helper + its keychain decode) and `security/mod.rs` (registers the new `iroh_pairing` module). Both are within the listed `security/` output.
- `api_server.rs` was **not** modified — `serve_iroh` / `CoreServiceSet` / `add_core_services` were reused exactly as 212 shipped them (the Iroh `CoreServiceSet` is assembled from the same boot handles in `boot.rs`).
- Boot now loads `managed.json` (via `load_managed_policy`) to read `remote_disabled()` for the Iroh `disable_remote` gate; it previously didn't load it. Read-only, best-effort (defaults to `false` on parse error), only consulted when the Iroh toggle is on.

**Open questions for next task (220 consumes this).**
- **Iroh-enable toggle (FROZEN):** env var `CONCERTO_ENABLE_IROH`, **default OFF**. Truthy = `1`/`true`/`yes`/`on` (case-insensitive). Exposed as `concerto_core::boot::ENABLE_IROH_ENV`. 220's split-host smoke flips this on for the Core under test.
- **Endpoint discovery:** a booted Core with the listener on exposes the live transport via `RunningCore::iroh() -> Option<&IrohRuntime>` (`{ transport, pairing_responder }`). The dialable id is `IrohRuntime::transport.endpoint_id()`; the Core's Noise responder static (the QR's responder static for the device IK handshake) is `transport.core_noise_public()`. **Also**: a `Devices.StartPairing` now stamps the live endpoint id into the FROZEN `PairingChallenge.lan_endpoint` field (previously empty), so an RPC client discovers where to dial from the pairing challenge itself — no new RPC. Boot also logs `iroh_endpoint_id=…` + `core_noise_public=…` at startup.
- **Pairing-over-`0x03` (the contract 220 + mobile/web build against):** the responder runs the Task-207 Noise XX (`Noise_XXpsk3_25519_AESGCM_SHA256`) over the one-shot token, then exchanges **4-byte-BE-length-prefixed** frames: device → `device_pubkey(32)||nonce(32)||signature(64)||device_name` (encrypted); Core → on-wire signed cert (`cert_bytes||signature`) on success, or a single `PAIRING_REFUSED_BYTE` (`0x00`) on rejection. This framing is what Task 217.5 locks (207 froze only the Noise handshake + signed-payload layout). The runtime pairing-start seam is `IrohPairingResponder::start_pairing` (mints token + arms the `0x03` listener + accept task); 220 / a future `Devices.StartPairing` wiring should drive **that** rather than the bare `PairingCoordinator::start_pairing` when Iroh is live.

**Deliberate debt.**
- **Revoke→teardown session keying (closing task: a 210/212 follow-up; on the Phase-2 Tier-3 checklist "revoke a device and confirm <60s stream teardown").** The shipped transport `serve_conn` keys an *accepted* Iroh session on the peer's **Iroh endpoint-id string** (`conn.remote_id()`), while `SessionCloser` is handed the **cert fingerprint**; the two never meet in shipped code (212 left this as boot/209's job, but the binding point that sees both is `serve_conn`, which is FROZEN — it has the `Connection` but not the cert; the gRPC auth layer has the cert but not the `Connection`/endpoint-id). So a *naturally-accepted* session is **not** severable by `revoke(fingerprint)` today. The live `IrohSessionCloser` wiring is correct (`fingerprint → DeviceId → IrohTransport::close_sessions_for_device`), and the Tier-2 test proves that chain end-to-end against a **fingerprint-keyed** session registered via the public `record_session_open`. Closing the gap needs a fingerprint↔endpoint-id binding seam (e.g. the auth interceptor publishing the binding when it validates a cert, paired with the transport recording sessions by fingerprint) — out of this task's frozen-surface scope; flagged for 220/a 210/212 revision and the manual checklist.
- No `TODO`/`FIXME`/`unimplemented!()`/`todo!()` introduced.

**Smoke-gate state: unchanged.** The Iroh listener is default-OFF (`CONCERTO_ENABLE_IROH` unset), so every existing smoke capability + the daemon's default behaviour is byte-identical; `scripts/smoke.sh` passes with exit 0. The new behaviour is reachable only with the opt-in toggle (which 220's smoke flips). The Core Noise X25519 static is stored in the OS keychain under the FROZEN slot `SecretKind::CoreNoiseStaticPrivateKey` (account `identity.core_noise_static`), distinct from the Ed25519 identity slot, as lowercase-hex of the 32 raw private bytes; keychain access is timeout-bounded (10s) + best-effort so a headless/keychain-less env degrades to "Iroh off" rather than hanging.
