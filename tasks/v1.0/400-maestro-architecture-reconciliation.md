# Task 400 — Maestro Architecture Reconciliation (design amendment: net-new `concerto-maestro-mcp` Core↔CLI MCP-stdio transport, Maestro-as-`AgentKind::Maestro` PTY session, strict-mode `ToolClass::ReadOnly`, Direct-API-deferred-as-Tier-1-seam)

| Field | Value |
|---|---|
| Phase | 4 |
| Task type | doc |
| Verification tier | 3 |
| Size | small (≤4h) |
| Depends on | — |
| Touches subsystem(s) | 08 (Maestro), 04 (Agent Supervisor) |
| Smoke gate | unchanged |

## Goal
Reconcile `design/08_Maestro_Agent.md` + `design/04_Agent_Supervisor.md` with the **built reality of `main`** before any Maestro code is written — the Phase-4 design-amendment insert, the 200 / 315.0 analogue whose Outputs are `design/` files (a `doc` task is the **one** task type allowed to edit `design/`). Today the design is **aspirational on four load-bearing points** and the code has **zero Maestro surface**: there is no `crates/core/src/maestro/`, no `maestro.proto`, no in-process MCP **server** (`concerto-mcp` / `concerto-maestro-mcp` do **not** exist — `crates/core/src/agent_supervisor/mcp.rs:54` `McpServer` + `McpScope` is read-only **config discovery**, NOT a server, and `rmcp` is not in the workspace); `resolve_agent_bin` (`crates/core/src/agent_supervisor/actor.rs:1818`) hardcodes `("claude", ["--dangerously-skip-permissions"])` and passes **no** `--mcp-config`/`--strict-mcp-config`/model/preamble/permission-mode; `AgentKind` (`actor.rs:66`) is `{Echo, Claude, Codex, Gemini}` with **no `Maestro`** variant (the `sessions.agent_kind` CHECK already allows `'maestro'`); and the built `PermissionResolver` maps **`(PermissionMode::Strict, _) => Decision::MustAsk`** for *every* tool (`crates/core/src/security/permission.rs:451`) — there is no auto-approve under strict — while `ToolClass` (`permission.rs`, table in `security/tool_classes.rs:42`) is the closed set `{Safe, Restricted, Dangerous}` with **no `ReadOnly`**. This task **amends the two design docs** (no code) to pin, as the Phase-4 ground truth that `design/08`/`04` will be read through: **(a)** the **net-new Core↔CLI MCP-stdio transport** — the Core hosts an **in-process `rmcp` stdio MCP server `concerto-maestro-mcp`** and the spawned CLI dials it via the **CLI's own `--mcp-config` + `--strict-mcp-config`** (so ONLY Maestro tools are visible); the existing `concerto-agent-host` is **PTY + CBOR-over-UDS terminal multiplexing and is NOT an MCP transport** — `design/08 §3.2`/`§3.6`'s "Core speaks MCP over stdio via the host" is made **concrete and distinct**; **(b)** **Maestro is a PTY-CLI session** under the Agent Supervisor — a new **`AgentKind::Maestro`** spawned through `start_session` with scratch `cwd = ~/concerto/maestro/`, `permission_mode = "strict"`, no fs/shell/net (`design/08 §3.1`'s vaguer "an agent process" → concretely a `start_session`); **(c)** the **strict-mode ReadOnly-auto-approve** rule — amend `design/04 §3.2`/`§3.10`'s permission matrix to add **`ToolClass::ReadOnly`** auto-approving the **11 read tools** under strict, while the **5 write tools + `propose_chip`** stay `MustAsk` → surfaced as the existing `AwaitingApproval`/`ResolveApproval` confirmation chip (carries `urgent`/`destructive_label`); **(d)** **Direct-API deferred as a FROZEN, unwired Tier-1 seam** (PHASE4_PLANNING **D1**) — the **3 CLI backends (Claude/Codex/Gemini) ship LIVE**, Direct-API is unwired in V1.0, and the **`enterpriseDataPrivacy=true` + external model ⇒ Maestro disabled** consequence (`design/08 §3.10`) is recorded, with on-prem Direct-API as a Tier-3 / fast-follow item. After this task, **401** implements the transport/permission/server amendment, **401.5** freezes the wire surface, **402** adds `AgentKind::Maestro` + `ToolClass::ReadOnly`, and **412** extends the provider seam (Codex/Gemini live + the Direct-API arm) — all reading `design/08`/`04` through this reconciliation rather than the idealized originals. **No code, no proto, no migration ships here** (those are Tier-1/Tier-2 tasks); the only thing this task makes true is that the design no longer contradicts the built seams, and the real-LLM behavior (digest quality, on-prem Direct-API) stays a Phase-4 Tier-3 / fast-follow concern.

## Inputs to read before starting
- `tasks/v1.0/PHASE4_PLANNING.md` §1 — **AUTHORITATIVE** decisions this task obeys and writes into the design: **D1** (CLI-first; Direct-API frozen Tier-1 seam; `enterpriseDataPrivacy`+external ⇒ Maestro disabled), **D2** (Maestro **is** a PTY-CLI session = `AgentKind::Maestro`), **D3** (net-new in-process `rmcp` stdio MCP server dialed via the CLI's `--mcp-config`/`--strict-mcp-config`; the agent-host is **not** an MCP transport), **D4** (strict-mode `ToolClass::ReadOnly` auto-approve). This file is the human-readable face of those four decisions in the canonical spec.
- `tasks/v1.0/PHASE4_PLANNING.md` §2 — the 401/402 sub-decisions this amendment must not contradict: module path `crates/core/src/maestro/` (not a new crate); `rmcp` is a `cargo deny` Stop-and-ask; `AgentKind::Maestro` touch-sites (`as_db_kind`/`resolve_agent_bin`/`from_db_kind`/parser-pack match/`parse_agent_kind`); the **no-op/structured parser pack** (Maestro tool calls ride MCP, **not** PTY-scrape — do not reuse `ClaudeCodePack`'s regex scraper); scratch cwd `~/concerto/maestro/`, no edit-mutex.
- `tasks/v1.0/PHASE4_PLANNING.md` §4.1/§4.3/§4.8 — the FROZEN contracts this design language must align to (the 16-tool MCP surface + transport = 401; the provider-selection seam = 402/412; `AgentKind::Maestro` + `ToolClass::ReadOnly` + scratch-cwd = 402 per 400). 400 **describes** these; it does **not** transcribe their Rust/proto (those are owned by 401/401.5/402).
- `tasks/v1.0/README.md` §5 (three-tier verification; §5.3 `doc` row = `markdownlint`/link-check if configured, else operator read) + §6 Phase-4 inventory (the 400 row + the Tier-3 manual checklist) + §4 amendment (Project→Workspace collapse — residual `project` naming survives only in MCP-scope vocab).
- `design/08_Maestro_Agent.md` §1, §3.1, §3.2, §3.6, §3.9, §3.10, §5.1, §6.1 — the sections this task amends: `§3.1` "an agent process" → `AgentKind::Maestro`; `§3.2`/`§5.1` the aspirational in-Core MCP server → concrete `concerto-maestro-mcp` rmcp-stdio transport dialed by `--mcp-config`; `§3.9`/`§3.10` the four backends + Direct-API-deferred + `enterpriseDataPrivacy` consequence; `§5.1` the strict/ReadOnly tool classification.
- `design/04_Agent_Supervisor.md` §3.2, §3.6, §3.10, §4.1 — `§3.6`/`R-9` "`concerto-mcp` … Core speaks MCP over stdio … via a pipe owned by `concerto-agent-host`" is the same aspiration (this transport **does not exist yet**) — reconcile it to the built reality alongside the Maestro twin; `§3.10` the permission matrix + the `PermissionResolver` `resolve()` pseudo-code (which already *names* `ToolClass::ReadOnly` in its `Normal` arm but the built code's `ToolClass` has no such variant and the `Strict` arm is unconditional `MustAsk`); `§4.1` `kind: AgentKind // claude | codex | gemini | maestro` already lists `maestro` in the doc.
- `crates/core/src/agent_supervisor/actor.rs:66` (`AgentKind`), `:1818` (`resolve_agent_bin` hardcode), `:368` (`start_session`/`StartSessionRequest`) — the **built** spawn reality the amendment cites verbatim as "Built-state". Read only these regions; do not survey the whole file.
- `crates/core/src/security/permission.rs:451` (`(PermissionMode::Strict, _) => Decision::MustAsk`) + `crates/core/src/security/tool_classes.rs:42` (`TOOL_CLASSES` table; `ToolClass` = `Safe`/`Restricted`/`Dangerous`) — the **built** permission reality the `(c)` amendment cites.
- `crates/core/src/agent_supervisor/mcp.rs:54` (`McpServer`) `:73` (`McpScope`) `:162` (`list MCP servers`) — proof that the only MCP code today is read-only **discovery**, NOT a server; the amendment must say so explicitly so 401 is not mistaken for "wire up the existing thing".
- `tasks/v1.0/305-cone-stats-suggest-seam.md` + `tasks/v1.0/313-vcs-provider-github.md` → "Handoff Notes" — the seam discipline + the dense, citation-heavy, FROZEN-marking register to match (305 = the unwired-Maestro-delegate `suggest_cones` seam this Phase-4 cluster later wires; 313 = the `cargo deny` Stop-and-ask precedent the `rmcp` add mirrors).

## Scope — in
- **Amend `design/08_Maestro_Agent.md`:**
  - **§3.1 (the Maestro is itself an agent process):** Make "an agent process supervised by 04" concrete: the Maestro is a **PTY-CLI session** spawned via the existing `AgentSupervisorHandle::start_session` (built signature: `StartSessionRequest{workarea_id, agent_kind, echo_text, cwd, permission_mode, resume_session_id} -> SessionId`, `actor.rs:368`) with a **new `AgentKind::Maestro`** variant, `cwd = ~/concerto/maestro/` (scratch dir, **not** a worktree, **no** edit-mutex), and `permission_mode = "strict"`. Cite that `AgentKind` is built as `{Echo, Claude, Codex, Gemini}` (`actor.rs:66`) with no Maestro arm and `resolve_agent_bin` (`actor.rs:1818`) hardcodes Claude — i.e. **402** adds the variant + spawn arm (not this doc). State the **no-op/structured parser pack** decision (tool calls ride MCP, not PTY scrape; do **not** reuse `ClaudeCodePack` regex scraping for tool calls — PHASE4_PLANNING §2).
  - **§3.2 + §5.1 (tool definitions: MCP server hosted in-Core):** Replace the aspirational language with the concrete **net-new transport**: the Core hosts an **in-process `rmcp` stdio MCP server named `concerto-maestro-mcp`** (module `crates/core/src/maestro/mcp.rs`, the FIRST MCP server in the codebase); the spawned CLI **dials it via the CLI's own `--mcp-config` (pointing at the stdio endpoint) + `--strict-mcp-config`** so ONLY the 16 Maestro tools are visible. Pin explicitly that **the existing `concerto-agent-host` is PTY + CBOR-over-UDS *terminal* multiplexing and is NOT an MCP transport**, and that `agent_supervisor/mcp.rs` (`McpServer`/`McpScope`, `:54`/`:73`) is read-only config **discovery**, not a server; `rmcp` is a net-new dependency (a `cargo deny` operator Stop-and-ask, 313 precedent). The 16-tool split (11 read / 5 write / 2 side-channel) stays as in `§5.1`; this doc only fixes **how** they reach the agent.
  - **§3.9 + §3.10 (cost: provider/model/budget + `enterpriseDataPrivacy`):** Add a **"V1.0 backend scope"** note (D1): the **three CLI backends (Claude / Codex / Gemini) ship LIVE** reusing the agent-host PTY machinery; the **Direct-API backend ships as a FROZEN, unwired Tier-1 seam** (a native function-call loop with no precedent in the codebase) wired in a fast-follow. Record the consequence: **`enterpriseDataPrivacy=true` + an external model ⇒ Maestro disabled** (the behavior `§3.10` already defines), and **on-prem Direct-API is a Tier-3 gate / follow-on** — so for V1.0, `enterpriseDataPrivacy=true` orgs get **routing only** (deterministic, no LLM), never an external-model digest. Note budget is **cumulative across backends** but is owned/wired by Task 403/412, not this doc.
  - **§3.6 (digest) cross-reference:** Add a one-line note that `next_step_chips`/`compute_deltas`/Sonnet-`complete` are **pseudo-code over not-yet-built seams** (the live V1.0 summarizer fallback is `DeterministicOneShot` via `OneShotLlm`; the real-LLM digest is Task 412's provider, judged at the Phase-4 Tier-3 gate) — so a future reader does not mistake the pseudo-code for a built API.
  - **Built-state reconciliation note:** Add a short **"Built-state reconciliation (2026-06-09)"** call-out block near each amended section (§3.1, §3.2, §3.9/§3.10) stating what exists on `main` today with `file.rs:line` citations and which Phase-4 task lands the real surface (401/402/412), so the amendment is self-evidently grounded.
- **Amend `design/04_Agent_Supervisor.md`:**
  - **§3.10 (permission modes — the matrix + `PermissionResolver` pseudo-code):** Amend the strict column to add **`ToolClass::ReadOnly`** — under `strict`, ReadOnly tools **auto-approve**; all other classes still `MustAsk`. State the **built** divergence to fix: `permission.rs:451` is `(PermissionMode::Strict, _) => Decision::MustAsk` for every tool (even `Safe` reads), and the built `ToolClass` (`tool_classes.rs:42`) is `{Safe, Restricted, Dangerous}` with **no `ReadOnly`**; the `§3.10` pseudo-code already references `ToolClass::ReadOnly` in its `Normal` arm, so this amendment makes the matrix and the enum consistent and extends ReadOnly-auto-approve into the `Strict` arm. Pin that **Task 402** adds the `ToolClass::ReadOnly` variant + the strict-matrix arm (not this doc). Classify for the Maestro toolset: the **11 read tools → ReadOnly (auto-approve under strict)**; the **5 write tools + `propose_chip` → MustAsk under strict** (surfaced as the existing `AwaitingApproval`/`ResolveApproval` confirmation chip carrying `urgent`/`destructive_label`; `notify_user` routes to 14, not a chip). **No bypass** of the write-tool confirmation (`design/08 R-2`).
  - **§3.2 / §3.6 / R-9 (the `concerto-mcp` in-Core MCP server):** Add the same reconciliation: the in-process MCP **server** + the Core↔CLI MCP-stdio transport **does not exist yet** (only read-only discovery does); `concerto-maestro-mcp` (401) is the **first** such server, and the CLI-dialed `--mcp-config` + `--strict-mcp-config` mechanism is the concrete transport for both `concerto-mcp` (workarea sessions, future) and `concerto-maestro-mcp` (Maestro, Phase 4). This keeps `04`'s "two distinct tool surfaces, two distinct roles" claim true while marking the transport as net-new.
  - **Built-state reconciliation note:** Add a short **"Built-state reconciliation (2026-06-09)"** call-out near §3.10 (permission matrix) and §3.6 (MCP) with the `permission.rs:451` / `tool_classes.rs:42` / `agent_supervisor/mcp.rs:54` citations and the owning Phase-4 task.
- **Cross-doc consistency:** keep terminology aligned with the Project→Workspace collapse (Workspace→Workarea→Session; residual `project` only in MCP-scope vocab like `~/.claude/.mcp.json` project scope) — do not reintroduce `Project`/`ProjectId` into the amended prose.
- Tests (Tier 3): **none automatable.** The verification is the `doc` gate — `markdownlint`/link-check if configured, else an operator read confirming the four reconciliations are present with built `file.rs:line` citations and a dated "Built-state reconciliation" note near each amended section. (No Rust/proto/SQL — this task ships zero code.)

## Scope — out
- **Implementing the `concerto-maestro-mcp` rmcp stdio server + the 16 tool schemas** — **Task 401** (adds `rmcp`, the module, the frozen schemas). 400 only pins the transport story in the design; it writes no Rust and adds no dependency.
- **Adding `AgentKind::Maestro` + the spawn arm in `resolve_agent_bin` + the provider-selection trait** — **Task 402** (the agent spine; Claude CLI live) and **Task 412** (Codex/Gemini live + the `DirectApiProvider` frozen-unwired arm). 400 leaves these as a described-but-unbuilt seam.
- **Adding `ToolClass::ReadOnly` + the strict-matrix arm in `permission.rs`** — **Task 402**. 400 amends only the design matrix; the enum variant + the `Strict` arm are code 402 writes (this task leaves `permission.rs:451` untouched).
- **The `maestro.proto` / `MaestroHandle` / `maestro.events` wire surface + the two-site `MaestroServer` registration** — **Task 401.5** (proto/wire freeze). 400 names them in prose only; it transcribes no proto.
- **`maestro_state` (migration 0015) / `chat_messages.metadata` (0016) / token accounting / the budget reset** — **Tasks 403 / 410 / 412**. 400 mentions "cumulative across backends" as design intent only; it adds no schema and reserves no migration.
- **Wiring the live Direct-API backend / on-prem (Bedrock-VPC, Vertex, Azure-Foundry) Maestro** — a **fast-follow + a Phase-4 Tier-3 gate item** (D1). 400 records the deferral and the `enterpriseDataPrivacy`-disabled consequence; it does not build the backend.
- **Real-world Tier-3 (the Phase-4 manual checklist line this defers to):** the operator reads the amended `design/08`/`04`, confirms the four reconciliations are accurate against `main`, and — at the Phase-4 gate — judges real-LLM digest quality + latency and confirms `enterpriseDataPrivacy=true` truly yields routing-only (no external-model digest); on-prem Direct-API is its own follow-on gate, not part of V1.0's automatable bar.

## Public interface this task locks
This task locks **no machine-checkable interface** (no Rust trait, no `*.proto`, no SQL, no TS) — it is a `doc` task whose Outputs are `design/08_Maestro_Agent.md` + `design/04_Agent_Supervisor.md`. What it **pins for the Phase-4 cluster to consume** are the four design reconciliations, each of which is *implemented and FROZEN by a later task* (this doc only commits the design language; it does not own the signature):

- **The net-new Core↔CLI MCP-stdio transport** (`concerto-maestro-mcp`, in-process `rmcp` stdio server, CLI-dialed via `--mcp-config` + `--strict-mcp-config`; the agent-host is **not** an MCP transport) — **described here; FROZEN by Task 401** (PHASE4_PLANNING §4.1). 400 does not transcribe the `rmcp` server type.
- **`AgentKind::Maestro` + scratch cwd `~/concerto/maestro/` + `permission_mode = "strict"`** spawned through the built `start_session` (`actor.rs:368`) — **described here; FROZEN by Task 402** (PHASE4_PLANNING §4.8). 400 does not add the enum variant.
- **`ToolClass::ReadOnly`** auto-approving the 11 read tools under strict, the 5 write tools + `propose_chip` staying `MustAsk` → confirmation chip — **described here; FROZEN by Task 402** (PHASE4_PLANNING §4.8, D4). 400 does not touch `permission.rs`/`tool_classes.rs`.
- **Direct-API as a FROZEN, unwired Tier-1 provider seam** (3 CLI backends live; `enterpriseDataPrivacy`+external ⇒ Maestro disabled) — **described here; FROZEN by Task 402, extended by Task 412** (PHASE4_PLANNING §4.3, D1). 400 does not define the provider trait.

The amended prose **consumes** these contracts as owned by 401/401.5/402/412 (per the cited PHASE4_PLANNING §4.N) and must say "FROZEN by Task NNN" rather than re-locking any signature itself.

## Implementation notes
- **The load-bearing rule: this doc records the built truth, not the wish.** Every amended paragraph must (1) state the design's prior aspiration, (2) state what `main` actually has with a `file.rs:line` citation, and (3) name the Phase-4 task that lands the real surface. The four anchors are exactly: `concerto-maestro-mcp` does not exist + `agent_supervisor/mcp.rs:54` is discovery-only (→ 401); `resolve_agent_bin` (`actor.rs:1818`) hardcodes `("claude", ["--dangerously-skip-permissions"])` and `AgentKind` (`actor.rs:66`) has no Maestro arm (→ 402); `(PermissionMode::Strict, _) => Decision::MustAsk` (`permission.rs:451`) + `ToolClass = {Safe, Restricted, Dangerous}` (`tool_classes.rs:42`) (→ 402); the four-backend table with Direct-API frozen-unwired (→ 402/412). Cite real symbols; never invent.
- **The MCP twin must be reconciled together.** `design/04 §3.6`/`R-9` makes the *identical* aspirational claim for `concerto-mcp` (workarea sessions) that `design/08 §3.2` makes for `concerto-maestro-mcp` — both say "Core speaks MCP over stdio via a pipe owned by `concerto-agent-host`", and **neither transport is built**. Amend both in the same pass so the "two distinct tool surfaces, two distinct roles" claim stays coherent: the net-new CLI-dialed `--mcp-config`/`--strict-mcp-config` stdio transport is the concrete mechanism for both; `concerto-maestro-mcp` (401) is the first instance.
- **Do not over-amend.** This is the *minimal* reconciliation: fix only the four points + the MCP-twin paragraph + the digest-pseudo-code disclaimer. Do **not** rewrite resolved-decision logs, sequence diagrams, or the 16-tool list (the tool **set** is correct; only its **transport** and **permission classification** are being made concrete). Leave `design/08 §3.3`-`§3.8` summary-cache/routing/condensation prose alone (those are accurate enough; 404/408/410 own them) except the one digest-pseudo-code disclaimer.
- **Keep the strict/ReadOnly classification consistent across both docs.** The 11-read/5-write/`propose_chip` split in `design/08 §5.1` must map cleanly onto `design/04 §3.10`'s amended matrix: read tools → `ToolClass::ReadOnly` (auto under strict); the 5 write tools + `propose_chip` → `MustAsk` (chip); `notify_user` → routed to 14 (not a chip). Use the same tool names in both docs.
- **Cross-platform / two-site / regen do not apply** (no code, no gRPC service, no proto). There is **nothing to regenerate** — `./scripts/regen-interfaces.sh` would produce an empty diff (no proto/Rust-api change), so the Verification omits the regen gate. Note this in the doc's own change so a later reader does not expect an interface diff from this commit.
- **Parallel build hint:** **solo** (doc task — one design author; the two `design/*.md` edits are a single coherent reconciliation pass, not a fan-out). Per `PHASE4_DAG.json`, task `400` has `fanout: 1` and write-set `design/08_*.md`/`design/04_*.md`, disjoint from every other Phase-4 task (it runs in Wave 1 alongside `403`).

## Verification
**Tier 3.** The `doc` §5.3 set (`markdownlint` / link-check if configured, else an operator read gate). This task ships **no code**, so there is no `cargo`/`pnpm`/`regen` gate.
1. `markdownlint design/08_Maestro_Agent.md design/04_Agent_Supervisor.md` → clean **if `markdownlint` is configured in the repo**; otherwise skip (the repo has no `.markdownlint*` config today — confirm, and if absent this step is an operator read, not a command).
2. Link-check the two amended files (relative `design/NN_*.md` cross-references + `tasks/v1.0/PHASE4_PLANNING.md` anchors resolve) → if a link-checker is configured; else operator confirms no broken intra-repo links were introduced.
3. **Operator read gate (the real bar):** confirm all **four** reconciliations are present in the design — (a) the net-new `concerto-maestro-mcp` Core↔CLI MCP-stdio transport via `--mcp-config`/`--strict-mcp-config` with the agent-host explicitly marked NOT-an-MCP-transport; (b) Maestro = `AgentKind::Maestro` PTY-CLI session, scratch cwd, strict; (c) the strict-mode `ToolClass::ReadOnly` auto-approve rule with the 11-read/5-write+`propose_chip` classification; (d) Direct-API-deferred-as-Tier-1-seam + the `enterpriseDataPrivacy`+external ⇒ disabled consequence — **each with a built `file.rs:line` citation** (`actor.rs:66`/`:368`/`:1818`, `permission.rs:451`, `tool_classes.rs:42`, `agent_supervisor/mcp.rs:54`) and a **dated "Built-state reconciliation (2026-06-09)" note** near each amended section.
4. Confirm `git diff` touches **only** `design/08_Maestro_Agent.md` + `design/04_Agent_Supervisor.md` (no code, no proto, no migration, no `docs/interfaces/`).
5. `scripts/smoke.sh` → **unchanged** (400 touches no smoke capability; this is a design edit).

**Tier-3 scope + what it does NOT cover.** This task is verified by an operator read (the `doc` tier), not CI — it proves the design *says* the right thing, not that any code *does* it. It does **not** cover: that `concerto-maestro-mcp` actually serves MCP over stdio (Task 401, Tier-1), that `AgentKind::Maestro` spawns with the right flags (Task 402, Tier-1), that `ToolClass::ReadOnly` auto-approves under strict in `PermissionResolver` (Task 402, Tier-1), or that a live external-model Maestro is disabled under `enterpriseDataPrivacy` (a **Phase-4 Tier-3 manual-checklist line:** "confirm budget-exhaust goes inert while routing still works" + the operator confirms `enterpriseDataPrivacy=true` yields routing-only / no external-model digest, and judges real-LLM digest quality + latency). The deferred on-prem Direct-API backend is its own fast-follow gate, outside V1.0's automatable bar.

## Definition of Done
- [x] `design/08_Maestro_Agent.md` amended: §3.1 (Maestro = `AgentKind::Maestro` PTY-CLI session, scratch cwd `~/concerto/maestro/`, strict, MCP-not-PTY-scrape tool calls); §3.2/§5.1 (net-new `concerto-maestro-mcp` in-process `rmcp` stdio server dialed by the CLI's `--mcp-config`/`--strict-mcp-config`; agent-host explicitly NOT an MCP transport; `agent_supervisor/mcp.rs` = discovery-only); §3.9/§3.10 (3 CLI backends live + Direct-API frozen Tier-1 seam + `enterpriseDataPrivacy`+external ⇒ disabled / routing-only); §3.6 digest-pseudo-code disclaimer
- [x] `design/04_Agent_Supervisor.md` amended: §3.10 (strict-mode `ToolClass::ReadOnly` auto-approve; 11-read/5-write+`propose_chip` classification; the built `permission.rs:451` `(Strict,_)=>MustAsk` + `ToolClass={Safe,Restricted,Dangerous}` divergence named, owned by 402); §3.2/§3.6/R-9 (the `concerto-mcp` MCP-stdio transport is net-new, not the existing discovery code)
- [x] A dated **"Built-state reconciliation (2026-06-09)"** note added near each amended section, each citing the built `file.rs:line` (`actor.rs:66`/`:368`/`:1818`, `permission.rs:451`, `tool_classes.rs:42`, `agent_supervisor/mcp.rs:54`) and naming the owning Phase-4 task (401/402/412)
- [x] Amended prose says "FROZEN by Task NNN" for the four reconciled contracts (per PHASE4_PLANNING §4.1/§4.3/§4.8) — it re-locks no signature itself; Project→Workspace terminology preserved
- [x] No TODO/FIXME/unimplemented!()/todo!() in new code (N/A — this task ships no code; the four reconciled seams return a typed Err/Status in their owning tasks, documented in Handoff)
- [x] No files outside Outputs modified (only the two `design/*.md`; no code/proto/migration/`docs/interfaces/`)
- [x] Interfaces regenerated + committed if any schema/contract changed (N/A — no proto/Rust-api/SQL change; `regen-interfaces.sh` would be an empty diff)
- [x] Single commit with the message below

## Outputs
- `design/08_Maestro_Agent.md` (modified — §3.1 Maestro-as-`AgentKind::Maestro`-PTY-session; §3.2/§5.1 net-new `concerto-maestro-mcp` rmcp-stdio transport via CLI `--mcp-config`/`--strict-mcp-config`, agent-host-not-an-MCP-transport; §3.9/§3.10 CLI-backends-live + Direct-API-frozen-Tier-1-seam + `enterpriseDataPrivacy`-disabled; §3.6 digest-pseudo-code disclaimer; per-section dated Built-state reconciliation notes)
- `design/04_Agent_Supervisor.md` (modified — §3.10 strict-mode `ToolClass::ReadOnly` auto-approve + 11-read/5-write+`propose_chip` classification; §3.2/§3.6/R-9 MCP-stdio transport marked net-new; per-section dated Built-state reconciliation notes)

## Commit message
```
phase-4: Maestro architecture reconciliation (design amendment)

Amend design/08 + design/04 to match the built reality before any
Maestro code lands: (a) the net-new Core↔CLI MCP-stdio transport
(in-process concerto-maestro-mcp rmcp server dialed via the CLI's
--mcp-config/--strict-mcp-config; the agent-host PTY+CBOR-over-UDS is
NOT an MCP transport, and agent_supervisor/mcp.rs is discovery-only);
(b) Maestro as an AgentKind::Maestro PTY-CLI session (scratch cwd,
strict); (c) the strict-mode ToolClass::ReadOnly auto-approve rule
(11 read auto, 5 write + propose_chip → confirmation chip); (d)
Direct-API deferred as a frozen Tier-1 seam (3 CLI backends live;
enterpriseDataPrivacy + external => Maestro disabled / routing-only).
Doc-only (the one task type allowed to edit design/); 401/402/412
implement the seams. Real-LLM behavior stays a Phase-4 Tier-3 item.

Refs: tasks/v1.0/400-maestro-architecture-reconciliation.md
```

## Handoff Notes (filled in when finishing)
- **Drift from plan:** —
- **Open questions for next task:** — (the consumers: **401** implements the `concerto-maestro-mcp` transport this doc describes — FROZEN surface PHASE4_PLANNING §4.1; **402** adds `AgentKind::Maestro` + `ToolClass::ReadOnly` + the strict-matrix arm + the Claude-CLI provider — FROZEN §4.8; **412** extends the provider seam with Codex/Gemini live + the `DirectApiProvider` frozen-unwired arm — FROZEN §4.3; **401.5** freezes the `maestro.proto`/`MaestroHandle`/`maestro.events` wire surface this prose names. Confirm each reads `design/08`/`04` through this reconciliation, not the idealized originals.)
- **Deliberate debt:** — (the four reconciled seams are *described* here and *built* later; their typed-Err/Status discipline — never `todo!()`/`unimplemented!()`, never empty-success — is enforced in 401/402/412, mirroring the 305 `ConeSuggestError::Unwired → Status::unimplemented` precedent.)
- **Smoke-gate state:** — (expected **unchanged**; 400 is a design edit touching no `scripts/smoke.d/*` or `scripts/smoke.manifest`, no code, no proto, no migration.)
